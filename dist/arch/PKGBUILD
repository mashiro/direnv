pkgname=direnv
pkgver=2.1.0
pkgrel=1
pkgdesc='Shell extension to manage your ENV'
arch=('x86_64' 'i686')
url='http://direnv.net'
license=('MIT')
makedepends=('go')
source=("$pkgname.zip::https://github.com/zimbatm/direnv/archive/v$pkgver.tar.gz")
sha256sums=('4dad14e53aa5a20fd11cdbb907c19a05f16464172af302981adb410bd691cefe')

build() {
  cd "$srcdir/$pkgname-$pkgver"

  source /etc/profile.d/go.sh
  make
}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  make install DESTDIR=$pkgdir/usr
}

# vim:set ts=2 sw=2 et:
